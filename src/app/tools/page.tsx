import { RoutePlaceholder } from "@/components/shared/RoutePlaceholder";

export default function ToolsPage() {
  return (
    <RoutePlaceholder
      description="The full searchable directory is intentionally scheduled for Part 03. The homepage search already uses the shared 15-tool catalog."
      title="All tools directory"
    />
  );
}
